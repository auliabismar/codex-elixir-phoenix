# Reference Samples

These routed manuals are condensed local samples aligned to the original upstream repo:

- `iflow-mcp/oliver-kriska-claude-elixir-phoenix`
- upstream root: `plugins/elixir-phoenix/skills/*/references/*.md`

Each local file mirrors an upstream filename and records its source path so the
router can inject real Phoenix/Elixir vocabulary without depending on the full
upstream corpus.

This directory is an upstream-aligned sample set for routing, not a full mirror
of the original 96-document stash described in the story notes.
