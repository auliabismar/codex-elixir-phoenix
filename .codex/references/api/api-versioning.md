# API Versioning

## Key Points
- Prefer additive changes inside a version and explicit version bumps for breaking changes.
- Keep deprecated fields readable until all supported clients have migrated.
- Document the versioning mechanism in every public integration guide.
