# API Telemetry

## Key Points
- Emit events for request duration, status code, and downstream dependency cost.
- Attach route or action metadata so dashboards stay explorable.
- Avoid logging full request bodies in telemetry handlers.
